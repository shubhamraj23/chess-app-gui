const ChessPiece = ({type}) => {
  return (
    <div className="piece">
      {/* Example: You can use an image for each piece
      <img src={`images/${type}.png`} alt={type} /> */}
    </div>
  )
}

export default ChessPiece
